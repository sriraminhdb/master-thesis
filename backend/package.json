{
    "name": "smart-home-washer-backend",
    "version": "0.1.0",
    "main": "src/index.js",
    "engines": {
        "node": "18"
    },
    "scripts": {
        "lint": "eslint . || true",
        "test": "jest || true",
        "start": "firebase emulators:start --only functions,firestore",
        "deploy": "firebase deploy --only functions"
    },
    "dependencies": {
        "axios": "^1.4.0",
        "express": "^4.18.2",
        "firebase-admin": "^11.10.0",
        "firebase-functions": "^4.3.0",
        "google-auth-library": "^8.7.0",
        "uuid": "^9.0.0"
    }
}